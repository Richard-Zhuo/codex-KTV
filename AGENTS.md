# 项目工作约定

- 修改JavaScript文件后必须运行 `npm test`。
- 安装依赖优先使用 `pnpm`；添加新的生产依赖前先询问确认。
- 项目信息用中文记录于本文件，保持结构清晰。

## 项目现状

金碧辉煌KTV手机操作演示，2026-09-19从空目录建立。原生HTML/CSS/JavaScript，无生产依赖；Node静态服务器，不是正式营业后端。2026-09-25初始化Git仓库，远端为`https://github.com/Richard-Zhuo/codex-KTV.git`，默认分支为`main`。

用户确认：店员全部用手机，老板兼任店长；首版先看可操作演示，原收款码配合手工确认，网络基本稳定；钉钉发布与接入暂缓。

## 入口与文件

- `node server.js` 或 `npm start`：默认4173端口，监听0.0.0.0供局域网手机体验，只提供白名单静态文件。店员系统入口为`/`，后台管理入口为`/admin`；两者共用本机演示状态，后台入口要求`backend.view`权限。
- `npm test`：Node内置测试，`rules.test.js`内48项业务用例，`theme.test.js`内4项主题用例，`database.test.js`内5项数据库契约用例，`entry.test.js`内1项入口契约用例，共58项。
- `rules.js`：商品、房型、角色、计价和事务规则。金额为整数分；克隆状态后校验，成功才返回；操作编号防重复提交。
- `app.js`：手机界面、手写签名和localStorage持久化（`jbhh-demo-v1`）。`index.html`提供店员系统，`admin.html`提供独立后台管理入口；两者共用业务应用和本机数据。
- `theme.js`：在样式表前初始化配色，使用独立偏好键`jbhh-appearance-v1`；“我的”页可选日间、夜间或自动，自动模式按演示日出06:00至19:00为日间，其余时间为黑金夜间。夜间基准色为深炭黑`#141210`、鎏金`#C9A227`、米白`#E8E0D0`，主按钮使用金属渐变。手动选择后持续生效，切回自动才恢复定时切换；与手动练习时间无关。回到页面和跨标签偏好变化会重新核对。
- 配色覆盖房卡、表单、弹窗、管理、报表及底部导航；签名区保留浅底便于看清笔迹。配色选择放在“我的”页，700px及以下单独一行，原有房卡600px断点保持不变。
- `README.md`、`店员练习手册.md`、`验收清单.md`：启动与边界、店员练习、真实测试证据。
- `database/`：PostgreSQL 16 关系型基线、当前门店主数据、金山营业报表原始暂存区、字段映射与导入模板。现有静态页面仍使用 localStorage，尚未连接数据库。
- 当前环境npm不在PATH；已通过 `pnpm --package=npm dlx npm test` 成功运行，无项目依赖变更。沙箱可能拒绝Node测试子进程；只读诊断可用 `node --test --test-isolation=none`，正式检查仍需执行npm test。

## 关键业务事实

- 9房：V01/V02/V03为小房；V05/V06为中房；333/666/999大房；888为VIP。无V04，不规范化房号。
- 白天14:00–18:00纯唱68/88/108元；夜间18:00–次日02:00标准套餐168/290/320元。基础房费和赠饮分账，不重复收钱。
- 普通赠饮12支/打、高端10支/打；销售整打均12支。2026-09-20用户要求取消开房“手工半打赠饮”入口；啤酒加购新增半打6支，价格为整打一半。
- 开房酒水增加“饮料、红青岛、蓝妹（罐装）”；商品显示名“金百威”改为“黑金百威”。
- 开房渠道默认为线下，可选美团／抖音；挂账和管理页保留开房渠道记录。
- 开房选择“饮料”时必须在开房表单直接配完实际酒水种类及总支数，按实际商品扣库；开房后的按钮称“换酒水”。套餐、付费增购与已赠酒水均可换，按支1:1、同级或向下、允许部分换且金额不变；瓶装水不可换出。饮料和汽水只单支加购。
- 加酒水类别界面合并为“啤酒”，其中仍可选择普通啤酒或高端啤酒商品；饮料、汽水、瓶装水单列。
- 营业账单提供“加其他”，可登记小吃、热食、烧鸡烤肉、代驾和其他；代驾填写金额，其他填写项目与金额。其他消费形成独立待收批次，可单独收钱，并计入结账、挂账、账单合计和报表“美食／其他”。
- 同一种可按打销售的酒水每增购24支，开单员／服务员可直接赠对应酒水半打；超出部分必须由具备赠酒审核权限的员工批准；审核本人申请还必须有`review.self`权限。赠送不增加应收，待确认申请会阻止结账或挂账。
- 瓶装水单支2元。
- 未建账商品只记流水，不校验、不预警；按商品期初盘点建账，不追扣历史流水。库管直接调整并在老板页提醒，不能期初建账。
- “收钱”只收最近一笔未收增购，没有增购待收时收开房费用；收钱后继续营业。美团／抖音平台券开房费用按0元计收，订单保留待接入扫码验券的数据接口；后续增购仍按实际金额收取。“结账”汇总剩余未收费用并在完成后转待清洁，结账少收的差额默认记录为免零金额；也可选择“特殊情况”，此时必须填写说明并由具备差额审核权限的员工复核；本人复核还需`review.self`权限。同一张加单可录入多种酒水；收钱与结账均支持微信／支付宝／现金／美团／抖音多笔付款并防重复提交。
- 所有演示身份均可从结账入口申请挂账，手机号或顾客姓名至少填一个，需填写备注并由经办员工签名；挂账金额只含尚未收取的余额，后台记录挂账经办、开单员工、预订员工及预订方式。≤1000元店长、>1000元老板审批；提单起24小时，不算实收。存酒独立，不抵欠款。
- 预订为今天／明天／后天／第N天后 + 下午场14:00—18:00／夜间场20:00—次日02:00；预订方式单选线下／手机／座机／美团／抖音。场次到时未开房，每小时在页面提醒录入预订的员工。
- 报表页按日／周／月切换并按房间汇总本机演示账单，只列当前统计周期内开过房的房间；房费单列，酒水消费包含开房赠饮，酒水赠送只统计后续赠送并显示具体酒水和打数，套餐赠饮不填写赠送人。表内金额统一带人民币符号；日报备注保留单据状态，周报和月报备注改为按日期倒序列出本周期挂账金额。报表同时显示买单方式、赠送人、美食散酒、烟、各类酒水打数、免零金额、合计和备注。美食散酒只统计付费美食及非整打／半打的付费酒水，套餐免费果盘和小吃不计入。暂未接入的烟等字段显示为空白。
- “我的”页按当前演示身份显示本人预订记录、已关联开房数和关联订单的酒水增购金额，作为订房与酒水提成的核对依据；未配置提成比例时不推算应发金额。
- 管理页支持有权限的管理人员按单个员工登记订房和增购酒水，订单保留员工归属与实际登记人；身份权限按单卡片和具体操作下拉选择调整。库存分为酒水支数与消耗品包数／份数，消耗品另记已开封数量；期初建账和每次盘点均先提交申请，由具备库存审核权限的员工批准后才改变账面数量；本人审核还需`review.self`权限。
- 房间状态支持“故障/维护中”，统一归类到房间页“异常”筛选。标记异常必须提交照片或文字证据，提交后立即生效且无需审批；恢复空房需再次提交证据，并由具备房间恢复审核权限的员工批准；本人审核还需`review.self`权限。异常及待恢复审核的房间都不能开房或预订。
- “我的”页增加采购入口，采购自动关联支出／报销；增加客诉／异常记录，可填写日期、房号、问题类型、描述并指定负责人。负责人提交处理结果和备注后，需具备客诉／异常恢复审核权限的员工批准才转为完成；本人审核还需`review.self`权限。未完成及待审核记录从每天14:00起提示。
- 交班除实点收款合计外，必须单独填写前台现金；前台现金仅留档，不重复计入营业实收差异。已回款挂账在“我的”和管理页显示为简洁折叠卡片，点击后再查看经办、开单、预订和备注详情。
- 挂账回款先登记金额与方式，由具备回款审核权限的员工批准后才扣减欠款并计入实收。挂账回款、特殊差额、超额赠酒水、房间恢复、库存盘点和客诉／异常恢复都要求对应业务审核权限；如果审核人就是提交人，还必须额外拥有`review.self`（允许审核本人申请）权限。批准或驳回均保留提交人、审核人、时间、备注及是否授权自审。
- 预订按未来场次记录，不占用当前房态；营业中的房间也可登记未来预订，同一房间可排多个不冲突场次。到店开房只接管当前匹配场次。
- “我的”页提供“支出／报销记录”入口，所有演示岗位默认可按日期、类型、金额、付款方式、性质、说明和可选图片凭证登记门店支出；采购、开单员、服务员、收银员、库管等低于财务的岗位只能查看本人记录，财务、店长、老板和管理员可查看全部记录。报销金额超过500元进入待老板审批，老板可批准或驳回；管理员仍可在具体权限中关闭某身份的查看、新增、查看他人或审批权限。记录保存在本机演示状态，性质包括一次性支出、固定支出、资金周转，付款方式沿用微信、支付宝、现金、美团、抖音。
- 房间页有未来预订的空房显示“已预订”并进入该筛选；卡片仍保留“点这里开房”以支持当前场次，提示只显示日期和场次。
- 存酒手机号或姓名至少填一个；支持一次添加多种酒，按手机号任意部分或姓名查询；取酒时用手机号最后4至11位或完整姓名核对。演示身份的岗位说明为：管理员（后台总管理）、卓老板（老板）、老板娘（店长）、邵老板（大堂经理）、雄老板（外联经理）、卓益（订房服务专员）、美娇（订房专员）；岗位名称只作说明，不替代具体权限。管理员可在“管理 → 身份权限”按开房、加酒水、收款、库存等具体操作调整其他演示身份权限，管理员权限固定；权限变更保存在本机演示状态并由业务规则强制执行。演示身份不等于真实登录安全。

## 边界与后续

基线文件位于 `C:/Users/KAMABOKO/WorkBuddy/2026-09-16-22-53-26/业务规则基线_v2.1.md`。同目录有PRD v1.3及治理版v1.1 Excel；本演示仅用明确饮品价格，未导入Excel、未修改源数据。文档中的旧历史说明不能覆盖v2.1最新确认。

各设备数据独立，无真实同步／支付／消息／备份／认证；练习时间可手动调整且不自动走时。交班是本轮累计练习核对。未做退款、并房、会员、存酒转赠过期及正式断网队列。

浏览器验收优先使用用户指定的browser-skill（bsk）。Edge专用会话曾验证主要营业闭环及部分取酒；增购换酒、分笔收钱／结账、挂账联系人和尾号取酒改由应用内浏览器实际走通。2026-09-27应用内浏览器已验证店员`/`与后台`/admin`均可访问、导航分离及自审权限选项显示。手机模拟固定底部点击存在工具坐标异常，键盘能正常导航；真实手机触控及大字体尚需现场验收。不要把规则测试通过夸大为生产就绪。
