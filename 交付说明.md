# 金碧辉煌 KTV 演示系统 · 交付说明

本文是新会话接手项目的总入口。先阅读根目录的 `AGENTS.md`，再按下面顺序查看分主题文档。

## 当前快照

| 项目 | 当前值 |
|---|---|
| 工作区 | `D:\iwen-codex\codex-KTV` |
| Git 远端 | `https://github.com/Richard-Zhuo/codex-KTV.git` |
| 默认分支 | `main` |
| 功能基线 | `6298dfc`；实际接手时以 `git rev-parse HEAD` 为准 |
| 技术形态 | 原生 HTML、CSS、JavaScript；Node 静态服务器；无生产依赖 |
| 店员入口 | `http://localhost:4173/` |
| 后台入口 | `http://localhost:4173/admin` |
| 业务数据 | 浏览器 `localStorage`，键 `jbhh-demo-v1` |
| 配色偏好 | 浏览器 `localStorage`，键 `jbhh-appearance-v1` |
| 数据库 | `database/` 中的 PostgreSQL 16 基线，尚未接入运行时 |
| 自动测试 | 58 项：48 项业务、4 项主题、5 项数据库契约、1 项入口契约 |

这是可操作演示，不是生产营业系统。页面中的收款、审批、库存、提醒和签名都只保存为当前浏览器的演示记录。

## 文档地图

1. [项目现状与交付结果](docs/handoff/01-项目现状与交付结果.md)：已经完成什么、验证结果和最近改动。
2. [需求与业务规则](docs/handoff/02-需求与业务规则.md)：不可遗漏的产品规则、身份和权限约束。
3. [代码架构与数据模型](docs/handoff/03-代码架构与数据模型.md)：文件职责、运行链路、状态和数据库结构。
4. [复现运行与验收](docs/handoff/04-复现运行与验收.md)：新会话从零启动、测试、浏览器检查和故障排查。
5. [未完成事项与风险](docs/handoff/05-未完成事项与风险.md)：生产化缺口、明确未做项和后续优先级。

现有补充文档：

- [README](README.md)：面向使用者的启动和功能说明。
- [店员练习手册](店员练习手册.md)：按场景练习操作。
- [验收清单](验收清单.md)：自动测试与浏览器验收记录。
- [数据库说明](database/README.md)：PostgreSQL 基线和历史导入流程。
- [金山报表字段映射](database/kdocs-import.md)：后续导入营业报表的规则。

## 新会话最短接手路径

```powershell
Set-Location D:\iwen-codex\codex-KTV
Get-Content AGENTS.md
Get-Content 交付说明.md
git status -sb
git log -5 --oneline
pnpm --package=npm dlx npm test
node server.js
```

启动后检查：

- `/` 显示“店员系统”，底部没有“管理”导航；有后台权限的身份可从右上角进入后台。
- `/admin` 显示“后台管理系统”，底部只保留“管理”和按权限显示的“报表”。
- 管理员打开身份权限时可以单独分配“允许审核本人申请”。

## 接手约束

- 修改任何 JavaScript 文件后必须运行 `npm test`。
- 安装依赖优先使用 `pnpm`；新增生产依赖前必须先询问用户。
- 不要把岗位名称当权限。业务操作必须调用 `rules.js` 的具体权限校验。
- 不要把 PostgreSQL 基线描述成已经接入；当前页面仍只用 `localStorage`。
- 不要把测试通过描述成生产就绪；真实认证、支付、同步、通知、备份和服务端事务仍未实现。

## 推荐的新会话提示词

> 工作区是 `D:\iwen-codex\codex-KTV`。先读取 `AGENTS.md` 和 `交付说明.md`，再按其中的文档地图了解现状。保持现有业务规则和演示数据，先运行测试确认基线，再处理新需求；修改 JavaScript 后必须运行 `npm test`。不要把本机演示误称为生产系统。
